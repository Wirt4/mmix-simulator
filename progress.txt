- Removed `editor-textarea` class from the hidden source textarea in `_text-field.html.erb` (visible styling no longer needed now that CodeMirror renders the editor; textarea is form-submit shim only). Added a request test asserting the class is absent.
- Wired `CodeMirrorInput` into the view and the controller (verifying the pre-existing wiring: `editorContainer` target on `_text-field.html.erb`, `editorContainerTarget` + `CodeMirrorInput` construction in `ide_facade_controller#connect`). Added two explicit integration assertions in `test/javascript/ide_facade_snapshot.test.ts` — that a `.cm-editor` (and `.cm-content`) is mounted inside the `editorContainer` target after connect, and that the hidden textarea's value seeds the CodeMirror document. Placed at the end of the describe block to avoid shifting CodeMirror's per-instance ͼ-style hashes baked into existing snapshots.
- Marked the "add `<div data-ide-facade-target="editorContainer">` above the textarea" task complete in `prd.md`. The div is already present at `_text-field.html.erb:2` and is covered by the existing `mounts a CodeMirror editor view inside the editorContainer target` test in `test/javascript/ide_facade_snapshot.test.ts` (asserts `.cm-editor` + `.cm-content` mount inside that target). Full Docker suite green: 87 Ruby tests / 209 assertions, 189 TS tests across 15 files.
- Marked three more already-complete tasks as done in `prd.md`: (1) `hidden: true` on the source textarea was already in `_text-field.html.erb`; added a request test `"show: source textarea has hidden attribute"` to lock it in. (2) Stimulus action bindings `syncScroll`, `updateLineNumbers`, and `updateHighlight` were already absent from all ERB views. (3) `editorContainer` was already in `static targets` and wired in `connect()` in `ide_facade_controller.ts`. Suite green: 10 Ruby tests, 189 TS tests.
