- Removed `editor-textarea` class from the hidden source textarea in `_text-field.html.erb` (visible styling no longer needed now that CodeMirror renders the editor; textarea is form-submit shim only). Added a request test asserting the class is absent.
- Wired `CodeMirrorInput` into the view and the controller (verifying the pre-existing wiring: `editorContainer` target on `_text-field.html.erb`, `editorContainerTarget` + `CodeMirrorInput` construction in `ide_facade_controller#connect`). Added two explicit integration assertions in `test/javascript/ide_facade_snapshot.test.ts` — that a `.cm-editor` (and `.cm-content`) is mounted inside the `editorContainer` target after connect, and that the hidden textarea's value seeds the CodeMirror document. Placed at the end of the describe block to avoid shifting CodeMirror's per-instance ͼ-style hashes baked into existing snapshots.
- Marked the "add `<div data-ide-facade-target="editorContainer">` above the textarea" task complete in `prd.md`. The div is already present at `_text-field.html.erb:2` and is covered by the existing `mounts a CodeMirror editor view inside the editorContainer target` test in `test/javascript/ide_facade_snapshot.test.ts` (asserts `.cm-editor` + `.cm-content` mount inside that target). Full Docker suite green: 87 Ruby tests / 209 assertions, 189 TS tests across 15 files.
- Marked three more already-complete tasks as done in `prd.md`: (1) `hidden: true` on the source textarea was already in `_text-field.html.erb`; added a request test `"show: source textarea has hidden attribute"` to lock it in. (2) Stimulus action bindings `syncScroll`, `updateLineNumbers`, and `updateHighlight` were already absent from all ERB views. (3) `editorContainer` was already in `static targets` and wired in `connect()` in `ide_facade_controller.ts`. Suite green: 10 Ruby tests, 189 TS tests.
- Removed `lineNumbers` and `highlight` from `TextFormatController.targets`. Deleted the Formatter dependency (Formatter required a lineNumbers element), removed `updateLineNumbers()`, `handleKeydown()`, `updateHighlight()`, `_updateHighlight()`, and `syncScroll()` — all dead after the target removal. Simplified `syncFromListing()` to a no-op guard. Created `test/javascript/text_format_controller.test.ts` with 5 assertions covering the removed targets and methods. Full Docker suite green: 194 TS tests across 16 files, typecheck clean.
- Migrated listing panel to read-only CodeMirror. Created `app/javascript/ide/code_mirror_listing.ts` implementing `IListing` via a permanently read-only `EditorView` (no `Compartment` needed). `setContents()` dispatches a full-doc replacement; `size` computes from the CodeMirror doc string with the same trim-trailing-newlines formula as the old `Listing`. Panel/button behavior (`default`, `toggle`, `unlock`, `isOpen`) is unchanged. Updated `ide_facade_controller.ts` to import and construct `CodeMirrorListing` instead of `Listing`. Added 13 unit tests in `test/javascript/ide/code_mirror_listing.test.ts`. Updated 5 stale snapshots in `ide_facade_snapshot.test.ts` (intentional: listing target now contains CodeMirror HTML). Full suite green: 207 TS tests across 17 files, typecheck clean.
- Migrated output panel to read-only CodeMirror. Created `app/javascript/ide/code_mirror_output_panel.ts` implementing `IOutputPanel` via a permanently read-only `EditorView`. `setValue()` dispatches a full-doc replacement and shows/hides the container based on whether text is non-empty; `clear()` resets to empty and hides. Removed the `<textarea class="output-textarea">` from `_output.html.erb` (no longer needed). Updated `ide_facade_controller.ts` to import and construct `CodeMirrorOutputPanel` instead of `OutputPanel`. Updated two snapshot tests that previously checked `outputTextarea.value` to check `.cm-content?.textContent` instead. Deleted and regenerated 6 stale snapshots (3 removed due to intentional output panel DOM change, 3 updated due to shifted CodeMirror ͼ-style instance hashes). Added 10 unit tests in `test/javascript/ide/code_mirror_output_panel.test.ts`. Full suite green: 88 Ruby tests / 211 assertions, 217 TS tests across 18 files, typecheck clean.
